# MIST-005 — World Model

**Статус**: ✅ Выполнено  
**Дата**: 2026-07-24  

---

## Назначение

Структура игрового мира с иерархией: Континент → Регион → Локация → Точка интереса.

## Модель

```
Continent (UUID, name, description, x, y)
  └── Region (UUID, name, continent_id, x, y)
        └── Location (UUID, location_id, name, region_id, x, y, z, connections, ...)
              └── PointOfInterest (UUID, location_id, poi_type, name, x, y, ...)
```

## Созданные файлы

### Модели

| Файл | Описание |
|---|---|
| `database/models/continent.py` | Континент (UUID, name, description, x, y) |
| `database/models/region.py` | Регион (UUID, name, continent_id, x, y) |
| `database/models/location.py` | Локация (UUID, location_id, region_id, x, y, z, JSON connections) |
| `database/models/poi.py` | Точка интереса (UUID, location_id, poi_type, name, x, y) |

### Сервисы

| Файл | Описание |
|---|---|
| `services/world_generator.py` | Генерация мира, карты, POI |
| `services/movement_service.py` | Обновлён для UUID и JSON connections |

### Тесты

| Файл | Описание |
|---|---|
| `tests/test_world_model.py` | 9 тестов моделей мира |

## Структура мира

```
Mistlands (континент)
├── Тёмный лес (6 локаций)
│   ├── Тёмный лес (0,0) — стартовая
│   ├── Логово волков (-1,1)
│   ├── Кровавый луг (-1,2)
│   ├── Топи ведьмы (-2,2)
│   ├── Забытое кладбище (-2,3)
│   └── Тёмная гавань (0,-1)
│
├── Побережье (5 локаций)
│   ├── Берег реки (1,0)
│   ├── Рыбацкая деревня (2,0)
│   ├── Подводная пещера (2,-1)
│   └── Покинутый лагерь (1,-1)
│
├── Горные земли (6 локаций)
│   ├── Древние руины (0,1)
│   ├── Хрустальная пещера (1,1)
│   ├── Обсидиановая башня (0,2)
│   ├── Вершина башни (0,3)
│   ├── Пепельные поля (0,4)
│   └── Заброшенная шахта (0,5)
│
├── Островки цивилизации (3 локации)
│   ├── Торговая площадь (2,1)
│   ├── Храм теней (3,1)
│   └── Теневой рынок (3,2)
│
└── Скрытые земли (7 локаций)
    ├── Библиотека эхов (1,2)
    ├── Зеркальный зал (2,2) — секретная
    ├── Белый лес (-1,3)
    ├── Замёрзшее озеро (-1,4)
    ├── Зачарованная роща (-2,4)
    ├── Врата Пустоты (4,1)
    ├── Сердце MIST (0,5) — финальная
    └── Узел порталов (0,6) — секретная
```

## POI (точки интереса)

| Локация | POI тип | Название |
|---|---|---|
| Рыбацкая деревня | shop | Рыбацкий склад |
| Рыбацкая деревня | quest_giver | Старый рыбак |
| Торговая площадь | shop | Торговые палатки |
| Храм теней | altar | Алтарь теней |
| Храм теней | quest_giver | Жрец |
| Библиотека эхов | quest_giver | Библиотекарь |
| Обсидиановая башня | shop | Лавка мага |
| Clockwork City | shop | Механическая лавка |
| Clockwork City | quest_giver | Инженер |
| Dragon Peak | altar | Гнездо дракона |

## ChronicleEvent интеграция

Все события мира публикуются в ChronicleEvent:
- `LOCATION_DISCOVERED` — открытие новой локации
- `LOCATION_VISITED` — посещение локации
- `region_id` — идентификатор локации в событиях

## Правила

1. Каждый объект имеет UUID
2. Координаты мира (x, y, z) для пространственной навигации
3. Connections — JSON список смежных локаций
4. Секретные локации требуют karma для доступа
5. POI привязаны к локациям

## Чек-лист

- [x] Entity (Continent, Region, Location, POI)
- [x] World Generator
- [x] Tests (9 тестов)
- [x] ChronicleEvent интеграция

## Зависимости

- MIST-004 (Player Core) — User.current_location ссылается на Location.location_id
- Все handlers используют MovementService для навигации

## Следующий шаг

MIST-006 — Combat System
